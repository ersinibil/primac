<?php require_once 'common.php'; topx('Cariler'); echo '<div class="panel" style="display:flex;gap:8px;flex-wrap:wrap"><a class="btn dark" href="contact_new.php">+ Yeni Cari</a><a class="btn" href="collection.php">+ Tahsilat</a><a class="btn" href="sales.php">+ Satış</a></div>'; try{$rows=db()->query("SELECT id,name,type,phone FROM contacts ORDER BY name LIMIT 100")->fetchAll();foreach($rows as $r)echo '<a class="item" href="contact_view.php?id='.(int)$r['id'].'"><b>'.htmlspecialchars($r['name']).'</b><br><small>'.htmlspecialchars($r['type']).' · '.htmlspecialchars($r['phone']??'').'</small></a>';}catch(Throwable $e){echo '<div class="err">'.htmlspecialchars($e->getMessage()).'</div>';} botx(); ?>